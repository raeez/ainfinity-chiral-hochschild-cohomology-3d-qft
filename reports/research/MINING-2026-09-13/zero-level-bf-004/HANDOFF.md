# Zero-level BF candidate 004

This candidate repairs the sole B1 blocker in the complete fresh 003 review, which is preserved as `review-003.md`. The local pairing and action densities are now defined before integration. The integrated pairing requires at least one compactly supported argument; the integrated action is defined on compactly supported fields. Stokes, variational Hamiltonian identities, gauge parameters and the nonzero CS comparison all carry their support domains explicitly. Both deciding divergent configurations appear in the reader text.

The unrestricted smooth field complex is preserved. Its local equations and cohomology are not replaced by compact-support cohomology. The actual compact observable complexes were already correctly defined. From the zero-level Green operator onward the complete source is byte-identical to003, including the quantum collar comparison at every level, full zero-level chiral extraction, and all auxiliary interval higher operations. `unchanged-mathematics.json` provides the exact boundary and hash.

The source remains standalone, without external-work citations, bibliography, or manuscript-production narration. The needed support argument and action-variation calculation are internal. `PROOF-B1.md` gives the deciding computations and scope; `primary-provenance.json` records the direct check of GRW v5 printed page 9, Notation 1, outside the reader text.

All 003 files remain unchanged. Its exact former reader source remains at `../zero-level-bf-003/source-snapshot/zero-level-bf.tex`; the live owned chapter now contains004. `parent-custody-final.json` verifies all preserved003 evidence hashes and all 33 accepted002 checks. The copied mining dispositions are byte-identical to003; no further stopped-source record is classified as consumed.

The standalone PDF has 13 pages. Every page and transition was rendered and inspected after the final source edit. The build has zero LaTeX errors, warnings, undefined references or overflow. The exact build logs, source/template snapshots, extracted PDF text and page hashes are frozen with the PDF. These checks do not constitute independent mathematical acceptance.

Build twice from the owned worktree:

```
TEXINPUTS=/Users/raeez/latex-template:platonic: pdflatex -halt-on-error -interaction=nonstopmode -output-directory=reports/research/MINING-2026-09-13/zero-level-bf-004/build reports/research/MINING-2026-09-13/zero-level-bf-004/main.tex
```

`versus-003.patch` is the exact bounded repair. `new-chapter.patch` adds the final standalone chapter to accepted002. `integration-proposal.patch` retains the unapplied driver and internal-navigation hunks. The accepted002 driver, chapter and bibliography were not edited. Root owns integration and its fresh source/PDF review.

The existing isolated worktree and branch remain `/Users/raeez/mathematics/worktrees/frontier-mine-vol2-20260913`, `repair/frontier-mine-vol2-20260913`, at base `7c0a1c3203b38dd47281d2dffd607f3d880e6056`. Only the owned chapter and new004 report subtree were written. No staging, commits, pushes, publication, delegation, shared-checkout edits or writes outside the worktree occurred. Fresh review must bind to004's exact manifest; no self-acceptance is claimed.
