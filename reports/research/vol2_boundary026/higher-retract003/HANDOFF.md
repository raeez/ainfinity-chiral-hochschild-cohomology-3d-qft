# Higher compact current retract

Candidate003 extends the actual compact projection and homotopy to compatible symmetric higher maps. It preserves the frozen sources and manifests of candidate001 and collision002. Fresh independent mathematical review remains required. The shared-template equation-reference defect remains a failed reader gate.

## Exact question and construction

Candidate001 supplies the actual compact quantum complex Q, its projection Pi=pi N_Q onto A=C[y][hbar], and H=N_Q^{-1}h0N_Q. Collision002 supplies the symmetric cumulant morphism on the full smooth quantum observable complex. The new question is whether the same supported projection and homotopy extend compatibly at every symmetric arity.

The first extension uses the actual full smooth moment map L_U=R N_U. Since R is a classical algebra map and chain map, the cumulants Lambda_n=C_n(L_U) form an L-infinity[1] morphism to the abelian algebra A. The annular inclusion is strict, and Lambda composed with that inclusion is exactly the identity coalgebra map. Its restriction to Q has coefficients C_n(Pi) and is a quasi-isomorphism because its unary coefficient is the accepted Pi.

The new homotopy uses polynomial forms Omega=C[tau,d tau]. This parameter is separate from both the physical interval coordinate and the curve coordinate. The form factor is first. Thus multiplication and differential use the explicit Koszul signs printed in the source.

On the classical current algebra define the algebra map

`P(y_i)=y_i, P(theta_i)=tau theta_i, P(delta_i)=tau delta_i+d tau theta_i`.

The last term is necessary: the differential of tau theta_i is exactly the displayed image of delta_i. Direct differentiation proves that P is a classical chain map. Conjugate it by the actual Gaussian operators:

`T=N_out^{-1} P N_Q`.

Then T is a chain map for the total quantum differential. Its cumulants `mathcal T=C(T)` give the compatible L-infinity[1] morphism. The endpoint maps are `i Lambda|Q` and the identity. The path fixes the annular inclusion and preserves the higher moment projection. These assertions follow as identities of coalgebra maps, using C(TS)=C(T)C(S).

For a monomial of total delta/theta degree r, the left d-tau coefficient of P is `tau^(r-1) sum_i theta_i partial_delta_i`. Its integral is the Euler homotopy h0. Therefore integrating the unary coefficient of T gives exactly the accepted compact homotopy H. The construction refines that particular homotopy and retains its actual compact supports.

## Deciding higher coefficients

The chain rule gives `K_out P=tau^2 P K_Q`, hence

`T=P exp(-hbar(1-tau^2)K_Q)`.

For current indices i,j, the first higher coefficient is

`mathcal T_2(x_i,x_j)=-hbar(1-tau^2)p_ij`.

Its differential is `2 hbar tau d tau p_ij`. The target quantum bracket on `T(x_i),T(x_j)` is its negative. Their exact cancellation is the first higher path identity. Keeping only the linear path would fail whenever the covariance is nonzero and tau is an interior parameter.

The first connected three-input coefficient is

`mathcal T_3(x_1^2,x_2,x_3)=2 hbar^2(1-tau^2)^2 p12 p13`.

It specializes to the higher moment coefficient at zero and vanishes at one. This calculation preceded the general path verification. The general coalgebra proof establishes all arities on finite words. All operations are polynomial in hbar on each polynomial input.

## Carrier and native bridge

The source Q is the embedded compact current complex in the native smooth boundary observables. Its odd generators are the actual compact primitive functions. The target is Omega tensor Q with total quantum differential and its second-order Leibniz defect. All chosen points, cutoffs, and supports remain fixed. No shrinking-support limit or product of point-supported distributions is used.

The higher moment map is defined on the full smooth boundary complex. The homotopy is asserted only on its specified current subcomplex Q. It does not identify the full smooth boundary complex with the finite polynomial algebra.

The final native paragraph connects the new map to the same L_U used by the supported interval chain-map proposition. It connects the new path to the same H used for actual separated-product primitives. Ordered interval operations and collision domains remain separate constructions. The two independently assigned children own those constructions and are not silently included in this result.

## Frozen artifacts

Source root: `research-candidates/vol2_boundary026/higher-retract003/`.

- New body: `chapters/current-higher-retract.tex`, SHA-256 `a81fedecc923ece21d6cbcb994f56edd8d594ce7303287409bb4b928ee85f48d`.
- Higher-operation consumer: `chapters/Volume_II_Free_Field_Higher_Operations.tex`, SHA-256 `1746491ef7a20eb86fa3a2f9309d42c7136de900b0ae4bc1a790f35b00c6508f`.
- Source aggregate: `ec06ca1c4fdf03961049ee6809380fd9fdc52054465df4c305b489cbe945a9be`.
- Manifest: `reports/research/vol2_boundary026/higher-retract003/manifest.json`, SHA-256 `6ec0575975ca0f628fe5949c3be6473b09087392b2ee3b767b0627458c217654`.

The higher-operation chapter includes the new body after the unchanged collision002 Taylor section. The BF and compact-current sources are unchanged. All original candidate001 and collision002 source hashes were verified before freezing. The base HEAD remains 7c0a1c3203b38dd47281d2dffd607f3d880e6056. No new external theorem dependency is used.

## Checks and exact limits

The executable check uses Python 3.14.6 with exact rational polynomial coefficients, including tau and the odd d-tau variable. It checks nine unary path examples, 81 binary equations, and 729 ternary equations. Inputs include odd primitive generators and products with multiple odd factors. It checks endpoint maps, integration to H, the printed binary and cubic coefficients, and target-bracket cancellation. All checks pass. The earlier contraction suite is not rerun. The arithmetic helper source and its hash are recorded in the calculation output.

The complete internal reader has 39 pages. Consecutive deterministic pdfLaTeX passes have identical PDF and AUX hashes. There are zero undefined references, overfull boxes, underfull boxes, or duplicate labels. All 326 final recorder inputs are archived and hashed. The new source and PDF text pass the manuscript firewall scan. Physical pages 37–39, including the complete new source and its preceding transition, were inspected as PNGs.

Actual equation destinations fail despite the clean logs. For example, `eq:current-cumulant-taylor` binds to `section.61`, and `eq:current-binary-taylor-identity` binds to `theorem.63`. The inherited compact equations have the same defect. The current shared template enables showonlyrefs. The existing equation-references template worktree owns the repair. `reference-gate.json` preserves the exact AUX lines and template hash. The source remains frozen, and this report does not claim reader acceptance before that repair and renewed destination checks.

The earlier collision002 render report preceded this finding. A supplemental `collision002/reference-gate.json` records it without changing that frozen source or manifest. The accepted mathematical contraction is not reclassified as a proof failure. Its reader must still receive the shared-template repair before further artifact acceptance.

## Residuals and custody

Fresh exact mathematical review is required for collision002 and this higher extension. Equation destinations require the existing shared-template repair. The full ordered interval comparison, collision-chain maps, residue-current products, and interacting quantum master equation remain outside this symmetric result. Arbitrary full-boundary cohomology completion is not asserted.

The operational requirement is gpt-6-astra with ultra. Independently observed runtime controls remain unavailable and unverified. No staging, commit, push, publication, native repository edit, central reader write, or standalone PDF opening occurred.
