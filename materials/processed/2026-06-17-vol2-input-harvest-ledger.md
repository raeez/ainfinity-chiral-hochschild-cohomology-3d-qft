# Vol II Input Harvest Ledger

Date: 2026-06-17.

Sources:

- `/Users/raeez/Desktop/Research Paper Refinement vol2.pdf`
- `/Users/raeez/Desktop/vol 2Manuscript Improvement Request.pdf`

This ledger records harvested mathematical corrections only.  A source
claim is harvested when the live manuscript or compute layer has
adopted the rigorous part as a proof, a conditional theorem, a scoped
hypothesis package, a retraction of the stronger claim, or a regression
guard against the false form.

## June 17 Manuscript Improvement Request

Status: harvested through A432--A434, A439--A440, the post-harvest
scope repairs A457--A462, and the final PDF W3 parity closure A466.

- A432 harvested the first-page theorem typing: chiral derived centre
  in the chiral bimodule category, HT realisation datum as input,
  directed \(\SCchtop\) dioperad typing, \(C_2\)-cofinite locus
  separation, and the Virasoro TQFT bibliography correction.
- A433 harvested the Fulton--MacPherson corrections: translation
  reduction, scale-reduced collision fibres, exclusion of the full
  complex-affine quotient from Stokes residues, closed/open
  non-consecutive divisor scope, and nested-corner typing.
- A434 harvested the residual theorem-level corrections: the
  standard-family BV realisation datum, KZ--Drinfeld--Kohno--
  Kazhdan--Lusztig line-braiding transport, typed FM propagation to
  spectral surfaces, the boundary--defect slab package, CFG/hCS lane
  separation, and chiral-versus-full-non-chiral gravity scope.
- A440 propagated the CFG/hCS lane separation back into the live
  spectral-braiding comparison remark: the CFG \(E_3\)-algebra and
  the Vol~II annular bar tower are compared only through a supplied
  trace-comparison datum and share, on standard lanes, a perturbative
  trace shadow.  The text no longer asserts a global comparison
  functor or an identification of ordinary Chern--Simons, hCS, and
  the chiral avatar.
- A457--A460 close the post-harvest status/scope drift exposed by the
  same correction layer.  A457 removes the undefined fused
  `\ClaimStatusProvedHereConditional` token from active theorem
  headers.  A458 keeps the Coxeter--Todd lattice calculation as a
  compatibility check for the Schellekens Type C row rather than a
  computation of the Kapustin--Saulina/DW BV anomaly class.  A459
  replaces audit prose inside the Three-Hochschild theorem by the
  positive comparison between chiral Hochschild data, ordinary
  mode/Weyl Hochschild concentration, and Gel'fand--Fuchs classes.
  A460 separates the DS--Hochschild class dichotomy's proved
  class-stratified criterion from the conditional class-\(\mathsf M\)
  cohomological hypotheses and the open class-\(\mathsf M\)
  chain-level HPL convergence problem.
- A461--A462 finish the adjacent finite-Zhu and source-truth cleanup.
  A461 removes the finite-Zhu-to-bounded-Massey overclaim and separates
  the logarithmic \(W(p)\) amplitude problem from generic non-\(C_2\)
  Virasoro/affine rows.  A462 removes residual audit markers from the
  live GCFT/Pic, oper, Three-Hochschild, and \(W(p)\)/irrational-coset
  surfaces, replacing them by positive scoped statements.
- A466 closes the final PDF \(W_3\) quartic odd-sector correction:
  the tree-level \(m_4(W,W,W,W)\) odd component is a parity-vanishing
  statement for a \(\sigma\)-equivariant contraction, not a proof
  sketch and not a non-zero coefficient forced by the weight-nine
  support envelope.
- Current rerun: the June 17 Manuscript Improvement guard bundle
  passes `96` checks across the FM, BV-datum, spectral-braiding,
  slab-package, CFG/hCS, half-space BV, and Universal Holography
  surfaces.

Remaining work from this source is not an unharvested correction.  The
full non-chiral gravity path integral, all-loop BV construction for
every family, and the existence of all components of the
CFG--hCS comparison datum remain explicit hypotheses or construction
problems.  A439 adds the same
discipline to the half-space reflected-weight theorem: reflected
Stokes weights are a conditional verifier for supplied renormalized BV
data, not a construction of the Costello effective action,
counterterms, or all-loop QME.  A440 records that the spectral remark
supplies only the conditional trace-shadow comparison, not a global
ordinary-CS/hCS/chiral-avatar comparison theorem.  A500 finishes the
rigorous harvest of that CFG/hCS item by naming the
positive datum
\(\mathfrak C_{\mathrm{CFG}\to\hCS}\) and obstruction profile
\(\mathfrak o_{\mathrm{CFG}\to\hCS}\): the missing theorem is now the
existence and compatibility of
\(\mathcal R_{\mathrm{ell}},\rho_{\mathrm{BV}},\rho_{\mathrm{fac}},
\tau_{\mathrm{tr}},\{h_n,c_n\}_{n\ge1},\nu_{\mathrm{Mell}}\), and the
optional \(K3\)/Hall--BKM component \(\eta_{\mathrm{K3}}\), not an
unnamed comparison slogan.

## June 10 Adversarial Finding Files

Status: critical harvestable celestial/THQG corrections closed through
A467--A471; non-rigorous or external construction demands remain
scoped as comparison data or construction problems.

- A467 removes the phantom Li--Strominger higher-soft support and
  replaces fake citation/equation-level coefficient claims by the real
  Hamada--Shiu and celestial soft-current comparison surfaces.
- A468 separates internal bar collision residues from physical
  celestial OPE coefficients: the affine level residue remains the
  symmetric \(k\kappa^{ab}\) channel, while physical gluon/graviton
  tree OPEs require the Mellin beta function and, for gravitons, the
  PRSY \(\bar z_{12}/z_{12}\) factor.
- A469 repairs the celestial moonshine bridge: the K3 symmetry action
  is geometric only for the actual sigma-model symmetry group
  \(G_\Sigma\); full \(\MtwFour\) twining enters through Gannon's
  virtual module.  Frame shapes are not injective class labels, the
  polar coefficient of \(h_g\) is \(-2\), the shadow is
  \(\Tr_{\mathbf{24}}(g)\eta^3\), and the full
  Rademacher/bar-depth comparison is conditional on explicit
  \(\mathcal R_n^{\mathrm{K3}}\) maps.  The umbral extension now cites
  Duncan--Griffin--Ono and is conditional on an umbral celestial
  realization datum.  The closing corollary is also conditional: it
  places Gannon's virtual Mathieu characters as celestial K3 matter
  traces and no longer claims a second proof-here derivation of
  Mathieu moonshine.
- A471 propagates the phantom-citation repair beyond the moonshine
  bridge: residual `DonnayFotopoulosPasterskiTaylor21` citations in the
  \(w_\infty\) endpoint and 3d-gravity soft comparison surfaces are
  replaced by live Strominger/Donnay--Puhm--Strominger/PRSY source keys,
  and the guard now forbids those retired keys across the active
  celestial source surfaces.

## June 8 Research Paper Refinement

Status: formula-level, scope-level, late desktop-export, and
post-sweep scalar/full corrections harvested through A470 where the
manuscript has a local theorem surface; open research programs are
recorded as such.

1. Two-coloured operations/no-open-to-closed: harvested on active
   \(\SCchtop\) surfaces; guarded by chain-dioperad tests.
2. Coderivations and suspension signs: harvested in the
   \(A_\infty\)-chiral and Hochschild coderivation models; guarded.
3. Sesquilinearity from translation equivariance: harvested on the
   repaired local chiral surfaces; remaining uses must cite the
   equivariance data.
4. FM residue signs: harvested in A433/A434; guarded by FM and
   spectral-braiding tests.
5. Arnold arity four: harvested with FM/Arnold guards; arity five is
   separate where not computed.
6. Higher homotopy tree formula: A318 prints the signed
   Boardman--Vogt cobar homotopy for Chiral Higher Deligne, and A435
   propagates the suspended Kadeishvili tree formula, internal-edge
   ordering, orientation line, and SDR side conditions to the live
   celestial boundary-transfer surfaces.  A437 prints the suspended
   HPL transfer formula on the DS--Virasoro gravity surface, including
   source/target suspension conjugation, internal-edge ordering,
   orientation line, and the rejection of a free \(\pm\)-choice.
   A443 fixes the low-degree ordered-bar
   \(\widehat{\mathfrak{sl}}_2\) computation: the degree-\(2\)
   differential now uses the ordered bar sign \((-1)^{i-1}\), the
   standard normalisation \([e,f]=h,\ [h,e]=2e,\ [h,f]=-2f\), and the
   explicit desuspended \(\Sigma_2\)-coinvariant sign
   \(s^{-1}e\otimes s^{-1}h=-s^{-1}h\otimes s^{-1}e\).
   A444 propagates the same sign convention upward to the general
   low-degree ordered-bar construction and the Heisenberg computation.
   A445 then fixes the adjacent scope error: the genuine open
   \(E_1\) differential is now written with the associative product
   \(\mu_2\), not with the vertex-algebra \(0\)-mode Lie bracket.
   The residue differential prints
   \(d_i=(-1)^{i-1}\partial_i\), the degree-\(2\) coefficient
   \(\sum \mu^K_{IJ}s^{-1}e_K\), the degree-\(3\) difference
   \(\mu_2(\mu_2(e_I,e_J),e_K)-\mu_2(e_I,\mu_2(e_J,e_K))\), and
   \(d(s^{-1}\alpha\otimes s^{-1}\alpha)=k\,s^{-1}(1)\).  The
   affine current constants \(c^K_{IJ;n}\) are now explicitly the
   vertex-shadow data controlled by the symmetric chiral/Arnold
   calculation.  A446 propagates the same correction to the
   Yang--Baxter-from-\(d^2=0\) boundary-face proof: the two contraction
   paths now apply \(\mu_2(a,b)\), \(\mu_2(\mu_2(a,b),c)\),
   \(\mu_2(b,c)\), and \(\mu_2(a,\mu_2(b,c))\), while the monodromy
   part supplies the \(R\)-matrix Yang--Baxter relation.  A447 carries
   the same separation into the bar-cohomology concentration theorem:
   the open-colour first differential is \(d_\mu\), the degree-one
   quotient is \(Q_{\mu_2}(\bar\cA)\), the Lie-homology depth
   sequence is only the symmetric chiral/Francis--Gaitsgory
   vertex-shadow sequence, and the leading \(r_0\) matrix is a vertex
   \(0\)-mode shadow rather than \(\mu_2\).  A448 applies the same
   discipline to the Yangian double-bar surface: the proved result is
   the PBW/current associated graded
   \(\operatorname{gr}_F Y_\hbar(\fg)\simeq U(\fg[t])\) and the
   primitive collision-residue bracket
   \(\operatorname{gr}_F d_{\mathrm{bar}}^{\mathrm{prim,coll}}
   =[-,-]_{\fg[t]}\), not a full equality
   \(\Barchord(Y_\hbar(\fg))\simeq U(\fg[t])\).  A449 propagates this
   into the executable oracles: the Yangian \(E_1\) product is the RTT
   associative multiplication \(\mu_2\), while the vertex
   \(0\)-mode/current bracket is only the primitive
   collision-residue shadow.  A450 then fixes the quantum-lattice
   comparison API: the full Yangian remains \(\hbar\)-visible in the
   RTT product and full ordered-bar input; only the PBW primitive
   collision-residue shadow loses \(\hbar\).  A451 scopes the
   quantum-lattice q-stability oracle itself: it is an explicit
   \(A_1\) finite-window benchmark, not a firstness theorem or a
   complete all-higher-operations q-stability result.  A452 separates
   the two quantum-affine/Yangian limits: bare \(q\to1\) gives the
   undeformed affine/loop object, while the Yangian arises only from
   the additive rational scaling degeneration of the trigonometric
   \(R\)-matrix.
   Statements needing a different tree homotopy remain conditional
   unless the corresponding data are printed.
7. \(E_3\)-PBW route: harvested as Rees-flat
   associated-graded/PBW-hypothesis theorem data where used.  A436 adds
   the non-tautology gate: \(W_x\) is the shifted
   \(E_3\)-tangent datum \(\mathbb T^{E_3}_{R_x,x}[-2]\), and
   \(H^\bullet(W_x[-2])\) must identify with the
   \(E_3\)-indecomposables of \(\operatorname{gr}_F R_x\).  General
   forms remain conditional.
8. BV fields/kernels/QME: harvested in A181/A320 and sharpened in
   A434 by the standard-family datum table and comparison maps.  A498
   closes the adjacent boundary-operation comparison scope: the graph
   residues \(m_k^{\mathrm{BV}}\) are compared with the bar
   coderivation only after a boundary SDR
   \(i_\partial,p_\partial,h_\partial\) transfers the BV operations to
   \(T^c(s^{-1}\bar A_\partial)\).  The live construction now prints
   \(D_A^{\mathrm{BV}}\) and the equality
   \[
   \pi_1D_A^{\mathrm{BV}}\big|_{(s^{-1}A_\partial)^{\otimes k}}
   =
   \pi_1D_A\big|_{(s^{-1}A_\partial)^{\otimes k}},
   \]
   and rejects the bare slogan
   \(m_k^{\mathrm{BV}}=m_k^{\mathrm{bar}}\) without the transfer datum.
9. Family normalisations: harvested across Heisenberg, affine,
   Virasoro, and \(W_3\) benchmark repairs; guarded by compute tests.
   A454 pins the Desktop-PDF \(W_3\) \(\Lambda\)-channel obligation
   directly: the live W3 surface must contain the literal
   \(P_\Lambda\) projection, coefficient-one \(WW\Lambda\) channel,
   FM pair-corner \(m_{4,\Lambda}\) kernel, primary-line
   \(\Lambda_0\) eigenvalue, matrix-valued \(K^{W_3}\), and the
   explicit statement that Gaudin commutativity requires a supplied
   \(\mathcal W_3\) Gaudin/RLL algebra rather than PVA Jacobi alone.
   A470 removes the remaining Heisenberg scalar/full overclaim from
   Rosetta, modular bootstrap, and the preface: the rigorous
   Faber--Pandharipande statement is
   \(F_g^{\mathrm{sc}}(\cH_k)=k\lambda_g^{\mathrm{FP}}\) on the scalar
   Hodge lane, not a bare full \(F_g(\cH_k)\) coefficient.  The same
   preface rung now separates the algebraic derived-centre bulk
   candidate from the physical HT bulk reading.
10. Virasoro pro/HPL tower: harvested as completed/pro ambient data;
    finite raw shortcuts are not accepted.
11. DS--Hochschild: harvested as completed chiral-bar
    coderivation-transfer/HPL data; A189 and A430 remove the generic
    Hochschild-functor shortcut.  A455 pins the BRST complex, SDR,
    conditional status, bounded-shift/weightwise convergence, and
    planar-rooted-tree brace transfer in the current input-harvest
    guard.
12. Higher-genus Arakelov/modular graph surfaces: harvested where
    local proof exists; otherwise explicitly conditional or scalar
    shadow scoped.  A455 pins the explicit relative modular graph
    theorem, genus-one Arakelov Green kernel and scalar tower, and
    tensor-Arakelov component formula/W3 channel matrix.
13. Anomaly curved MC: harvested on 2026-06-08 with
    \(d\eta+\eta^2+\Theta=0\) separated from the strict central-shadow
    convention.  A455 guards the curved-MC anomaly-completion
    definition and Virasoro class-\(\mathbf M\) example.
14. K3/Borcherds operator square: not solved as an operator
    construction; harvested as a scoped construction problem rather
    than a scalar modular identity.  A438 fixes the conditional
    square's typing: the Hall side starts from the K3 fibre source,
    while the chiral side passes through
    \(D^b\mathrm{Coh}(K3)\xrightarrow{(-)\boxtimes\mathcal O_E}
    D^b\mathrm{Coh}(K3\times E)\), then \(\PhiFA_3\),
    \(\SpCh_{E,C}\), and \(\Zderch(A_X)\).  A442 propagates this
    typed square to the main K3\(\times E\) bridge summary: the
    protected-Pfaffian operator form is now explicitly conditional on
    the finite Hall and chiral-comparison gates and no longer sends
    \(D^b\mathrm{Coh}(K3)\) directly through an unindexed \(\PhiFA\).
    A455 guards the theorem as conditional on the P1 datum,
    \(\hypAmbientWtCpl\), \(\effPfaffOrient+\effPBWnoExtra\), and the
    finite Hall gate system, not as a scalar-character theorem.
15. Celestial residue theorem: analogy-only language is rejected;
    residue-theorem statements are used where supplied, otherwise the
    bridge remains conditional.  A441 repairs the four-point MHV
    surface: the Parke--Taylor expression is now the value of a named
    cyclic FM boundary residue datum, with colour-order projection,
    spinor-helicity residue characters, negative-helicity pairing, and
    orientation convention supplied as hypotheses.  The theorem no
    longer claims an unqualified physical scattering-amplitude
    reproduction or a general-\(n\) Koba--Nielsen consequence.
16. Gravity split: harvested through A196, A432, A434, and intervening
    gravity guards; exact chiral algebraic claims are separated from
    conditional non-chiral analytic path-integral data and heuristic
    physical readings.

### Desktop 05:57 Extended Local-Theorem Fragments

The 05:57 Desktop export also contains a later local-theorem fragment
layer after the primary sixteen obligations.  A453 indexes the
harvestable rigorous part of that layer and guards it in
`compute/tests/test_vol2_input_harvest_guards.py`.

1. Spectral discriminants are cyclic transport modules, not scalar
   numbers: active in
   `chapters/connections/casimir_divisor_core_transport.tex` through
   \(M(p_A)=k[t]/(p_A)\), divisor cores, and the no-projector warning.
2. The Casimir recurrence module is the reduced
   \(H^{1,\alg}_{\red}(A)=\mathcal R_A^\vee\): active in the same
   divisor-core transport chapter.
3. Relative Hochschild duality is a \v Cech duality theorem: active in
   `chapters/connections/ym_higher_body_couplings.tex` via relative
   Hochschild cochains, \(W\)-boundary \v Cech centres, and the
   higher-body bridge datum.
4. Instanton completion is Novikov-completed Koszul duality: active in
   `chapters/connections/ym_instanton_screening.tex`.
5. Screenings are a Hodge theorem plus a gap inequality: active through
   the screening Hodge theorem, spectral-gap criterion, and internal
   screening domination package.
6. Celestial transfer is an obstruction tower, not a dictionary:
   active in `chapters/connections/celestial_boundary_transfer_core.tex`,
   including obstruction classes, recursive linearization, spectral
   obstruction, Airy--Witt, and the four-point residue datum.
7. The chiral Chevalley--Eilenberg algebra has generators, relations,
   factorization descent, Disk-\(3\) reduction, Fubini, sunrise, and
   6d hCS reduction surfaces in
   `chapters/connections/chiral_ce_factalg_gen_rel.tex`.
8. The six-dimensional hCS coefficient tower is graph-complex data,
   active in `chapters/theory/six_d_hcs_feynman_coefficients.tex`.
9. The Kontsevich integral is a restricted-operation theorem: active in
   `chapters/connections/kontsevich_integral.tex`; real-locus
   restriction and boundary normalization are not consequences of the
   Feynman transform alone.
10. Non-principal \(W\)-algebras use orbit-sensitive bar data:
    active in
    `chapters/connections/fractional_ghost_chain_level_platonic.tex`
    through good gradings, branched-cover integralization, and
    Galois descent.
11. Supersymmetric indices are annular-bar supertraces: active in
    `chapters/connections/ordered_associative_chiral_kd_frontier.tex`
    through the refined annular bar complex, annular index, \(N=2\)
    elliptic genus, weak Jacobi form, and \(N=4\) index statements.
12. Chiral Springer resolution resolves the singular Steinberg object
    before any Yangian identification: active in
    `chapters/connections/spectral-braiding-frontier.tex`, with the
    Yangian statement explicitly conjectural.
13. The Yang--Mills boundary lane includes central formality and
    mixed-coupling theorems: active in
    `chapters/connections/ym_boundary_theory.tex`.
14. The one-line replacement layer is harvested as anti-overclaim
    discipline: product-formal \(\SCchtop\) recognition, completed
    class-\(\mathbf M\) ambient, Virasoro operator-valued \(R\)-matrix
    correction, conditional Page/Stokes data, pro-window
    \(W_\infty\), conjectural harmonic \(\beta_N\), arity-four
    \(m_4\) scope, bulk--boundary cone reconstruction, one-wheel
    genus-one modular class, and bar-valued logarithmic superconnection
    are all pinned by the same A453 guard.

Latest focused verification: A434 passed `44` harvest guards, A435
passed `41` celestial-transfer/Boardman--Vogt guards, A436 passed
`67` CHD/deletion-ledger guards, and A437 passed `210` gravity-HPL
guards with `1` skipped.  A438 passed `27` P1 protected-Pfaffian
guards.  A439 passed `44` affine half-space BV guards.  A440 passed
`2` CFG/hCS lane-separation guards.  A441 passed `36` celestial
boundary-transfer guards.  A442 passed `28` protected-Pfaffian
Hall/chiral-square guards.  A443 passed `51` ordered chiral \(K_d\)
guards.  A444 and A445 each passed `52` ordered chiral \(K_d\)
guards.  A446 passed `53` ordered chiral \(K_d\) guards.  A447 passed
`54` ordered chiral \(K_d\) guards.  A448 passed `98` ordered
KD/double-bar guards.  A449 passed `99` ordered KD/double-bar guards.
A450 passed the quantum-lattice oracle's `19/19` internal checks and
`99` ordered KD/double-bar guards.  A451 passed the quantum-lattice
oracle's `19/19` internal checks and `100` ordered KD/double-bar
guards.  A452 passed the quantum-lattice oracle's `19/19` internal
checks and `101` ordered KD/double-bar guards.  A453 passed `17`
input-harvest source guards; after A454 the same input-harvest guard
passed `18` source checks, and the paired input-harvest/W3 bundle
passed `81` checks.  A455 extends the same input-harvest guard to
the mid-PDF A188--A194 block, passes `21` source checks, and the
paired ownership bundle passes `267` checks.
A457--A460 remove the undefined fused
`\ClaimStatusProvedHereConditional` token from active theorem headers,
separate Schellekens Type C lattice compatibility from BV/DW anomaly
construction, remove audit prose from the Three-Hochschild theorem,
and make the DS--Hochschild class dichotomy a proved
class-stratified criterion rather than a fused conditional-status
phrase.  A461 repairs the finite-Zhu/Massey tempering lane: finite
Zhu data no longer prove bounded Massey growth, generic Virasoro and
generic affine rows are no longer counted as \(C_2\)-cofinite inputs,
and the logarithmic \(W(p)\) frontier is stated as an explicit
regular/logarithmic amplitude problem.  A462 replaces residual
protocol/all-caps audit markers by scoped GCFT/Pic, oper,
Weyl/Gel'fand--Fuchs, finite-envelope \(W(p)\), and finite-Zhu/BGG
provenance statements.  A463 repairs the adjacent gravity
topologization scope: logarithmic \(\mathcal W(p)\) is no longer
classified as an inherited finite-orbifold/coset
\(E_3\)-topological row, and remains a frontier row until its
logarithmic HT datum, antighost/topological lift, descent data, and
regular/logarithmic amplitude bounds are supplied.  A464 tightens the
high-genus gravity trace: \((G2)=(G3)\) at \(g\ge2\) is only the
scalar-shadow comparison in the weight-completed ambient with
\(\hypAmbientWtCpl+\effScalarShadowProj\); the original-complex
upgrade remains Frontier F5.  A466 closes the remaining PDF \(W_3\)
odd-sector proof-surface item by parity vanishing.  A467--A471 close
the adjacent adversarial celestial/THQG citation, OPE-transfer,
moonshine-bridge, and source-propagation overclaims.  A470 closes the remaining Heisenberg
scalar/full export drift.  Current reruns: the consolidated
input-harvest guard cluster passes with `490 passed`; the adjacent
post-harvest status/scope cluster passes with `392 passed, 1 skipped`;
the focused finite-Zhu/logarithmic/programme bundle passes with `41
passed`, and the focused A462 source-truth suite passes with `28
passed`; the focused A463 gravity guard passes with `211 passed, 1
skipped`; the focused A464 programme/gravity/curved-Dunn bundle passes
with `228 passed, 1 skipped`; the focused A470 Heisenberg
scalar-linearity suite passes with `10 passed`; the focused A471
gravity source guard and widened DFPT guard pass with `1 passed` each.
`make verify-licensing` reports zero blockers and zero
warnings.  The latest `make fast` converged after two passes at
2523 pages with zero undefined citations, zero undefined references,
and zero rerun requests.

A472 closes the active bibliography/source-truth residue left after the
mathematical harvest.  The live surface no longer carries the
`TODO: librarian verification` cluster.  The `BMP-Wsymmetry` entry is
now treated as the Bouwknegt--Schoutens 1993 Physics Reports review in
both prose and independent-verification decorators; the separate
de Boer--Tjin CMP 160 relation paper is cited through
`deBoerTjinRelation93`; the wrong `CLNS24`/`arXiv:2404.02410` hook-type
reference is removed in favour of the verified `CLNS25` convolution
paper; and the misleading `FF90` critical-level route is replaced by
the canonical `FF92` entry.  The A472 source-truth/topologization
bundle passes with `37 passed`; `make verify-licensing` reports zero
blockers and zero warnings; fixed-string stale scans and scoped
`git diff --check` are clean.

A473 closes the log-HT reduction-datum overclaim and the adjacent affine
monodromy comparison residue.  The degree-zero line-complex hypothesis
now gives an `\h`-adic strong deformation retract only after a
compatible tree-level Hodge contraction/Hermitian metric has been
chosen; the resolved datum and monodromy theorem are functorial in that
chosen reduction data, and the old `thm:canonical-retract` label is
retired from the active core.  The affine IV surface no longer states
that the programme spectral `R`-matrix is pointwise equal to the
quantum-group `R`-matrix; it records the braided-monoidal comparison
through the KZ/Drinfeld associator and gauge normalization.  The
focused log-HT/affine bundle passes with `42 passed`, the full
input-harvest guard passes with `28 passed`, `make verify-licensing`
reports zero blockers and zero warnings, fixed-string stale scans over
the live sources are clean, and scoped `git diff --check` is clean.

A474 propagates A473 to the active spectral-braiding bridge.  The
spectral-to-categorical proposition now fixes a compatible tree-level
Hodge contraction before forming the reduced line category, and its
proof says that Theorem `thm:unconditionality` gives an `\h`-adic SDR
relative to that choice rather than a canonical retract.  The paired
input-harvest/spectral-braiding test bundle passes with `53 passed`;
fixed-string stale scans over active sources find no remaining
canonical-retract wording, `make verify-licensing` reports zero
blockers and zero warnings, and scoped `git diff --check` is clean.

A475 repairs the \(M2\) genus-one scalar in the broader programme
surface.  The \(M2\) component theorem now keeps
\(\Theta_{M2,1,0;0}=\kappa_{M2}\omega_1\) symbolic.  The numerical
value \(-K^2/12\) is proved only as the zeta-regularized benchmark
\(\kappa_{M2}^{\zeta}=K^2\zeta(-1)\), and the comparison
\(\kappa_{M2}=\kappa_{M2}^{\zeta}\) is a named conjecture.  The
downstream M2 modular-Koszul, globalization, and Swiss-cheese tables
now print the benchmark plus comparison conjecture rather than an
unconditional scalar row.  The focused M2 bundle passes with
`3 passed`, the paired input-harvest/M2 bundle passes with `39 passed`,
fixed-string stale scans are clean outside negative guard strings,
`make verify-licensing` reports zero blockers and zero warnings, and
scoped `git diff --check` is clean.

A476 repairs the active gravitational \(S\)-duality wording.  The exact
theorem remains exact, but it is now explicitly the algebraic
Koszul--Verdier identity
\(\sigma(\Theta_\cA)=\Theta_{\cA^!}\) on the chiral surface.  The text
no longer imports a physical Montonen--Olive-style duality, real form,
analytic sewing, or dynamical-metric statement into that theorem.  The
focused S-duality guard passes with `1 passed`, the consolidated source
guard passes with `30 passed`, fixed-string stale scans are clean over
the live S-duality file, `make verify-licensing` reports zero blockers
and zero warnings, and scoped `git diff --check` is clean.

A477 repairs the abelian Chern--Simons worked example after the
affine-monodromy harvest.  The proposition now fixes the Gaussian HT
gauge data, compatible Hodge reduction, and KZ/Drinfeld normalization
before invoking the affine bridge; the proof says the comparison with
\(\mathrm{Rep}_q(\mathfrak g)\) is braided-monoidal on the fixed
evaluation surface, not an unconditional pointwise \(R\)-matrix
identity.  The focused A477 guard passes with `1 passed`, the
consolidated input-harvest guard passes with `31 passed`, the paired
examples plus affine IV bundle passes with `48 passed`, and
fixed-string stale scans over the worked example are clean.

A478 repairs the foundations converse of bicoloured-primitive
universality.  A coherent bicoloured nine-tuple reconstructs a
canonical \(\SCchtop\)-algebra, equivalently the local algebraic
bulk--boundary shadow of a \(3\)d HT theory; it does not by itself
construct a physical HT QFT.  The physical interpretation now
explicitly requires separate BV/HT realisation, descent, and anomaly
data.  The focused A478 guard passes with `1 passed`, the consolidated
input-harvest guard passes with `32 passed`, the programme holography
gate passes with `6 passed`, fixed-string stale scans find the retired
physical-HT wording only in negative guard strings, `make
verify-licensing` reports zero blockers and zero warnings, and scoped
`git diff --check` is clean.

A479 repairs the Type-A Baxter--Rees boundary-germ scope.  The formal
braided boundary germ is now stated relative to supplied polynomial
\(R\)-matrix and Theta-associator tensor data; the continuation
principles are internal only after those polynomial tensor inputs have
been fixed, and the boundary-germ corollary explicitly assumes the
two continuation theorems' hypotheses.  The focused Type-A obstruction
suite passes with `6 passed`, and fixed-string stale scans find the
retired canonical/unconditional phrases only in negative test
assertions.

A480 propagates the affine-monodromy scope repair into the active
spectral-braiding frontier.  The Drinfeld--Kohno ladder remark now says
the affine finite-braiding comparison is proved on the fixed reduced
evaluation comparison surface, after compatible Hodge reduction and
KZ/Drinfeld normalization; it is a braided-monoidal comparison with
\(\mathrm{Rep}_q(\mathfrak g)\), not a pointwise equality of spectral
and quantum-group \(R\)-matrices.  The focused affine guard passes with
`1 passed`, and fixed-string stale scans find the retired
unconditional-affine sentence only in the negative guard assertion.

A481 repairs the Unified Chiral Quantum Group climax anchor.  A
verified fibre satisfying the Universal Holography hypotheses now
supplies an algebraic \(3\)d HT bulk--boundary extension
\(T_A^{\mathrm{alg}}\) only through the specified HT realisation datum;
its physical gauge-theory interpretation requires the same BV/HT
realisation and descent data and is not a consequence of the boundary
chiral algebra \(A\) alone.  The unified-QG guard suite passes with
`10 passed`, and fixed-string stale scans find the retired canonical
gauge-theory phrases only in negative test assertions.

A482 repairs the active spectral-braiding core.  The local line
category now treats \(R(z)\) as a meromorphic exchange family satisfying
the Yang--Baxter relation with the associator term specified by
Theorem~\(\ref{thm:YBE}\); the ordinary braided-monoidal hexagon is
reserved for the descended centre-side category after
evaluation/descent along an exchange path.  The focused guard passes
with `1 passed`, the paired spectral-braiding plus consolidated
input-harvest bundle passes with `57 passed`, `make verify-licensing`
reports zero blockers and zero warnings, and scoped `git diff --check`
is clean.

A483 aligns the factorisation quantum-group definition with A482.  The
local object is now defined as a local spectral tensor datum: line
category, meromorphic products \(\otimes_z\), exchange isomorphisms
\(R_{V,W}(z)\), and a three-point associator \(\Phi_A\) satisfying the
associator-controlled Yang--Baxter relation.  Strict QYBE appears only
after choosing a comparison surface with \(\Phi_A=1\), and before
evaluation/descent the datum is not an ordinary braided monoidal
category.  The focused spectral guard passes with `1 passed`, the
paired spectral-braiding plus consolidated input-harvest bundle passes
with `57 passed`, `make verify-licensing` reports zero blockers and
zero warnings, and scoped `git diff --check` is clean.

A484 propagates the same associator discipline to the active
line-operator module-category theorem.  The chosen line surface now
carries a local spectral tensor datum
\((\otimes_z,R_{V,W}(z),\Phi_\cA)\), not merely a flat spectral
coproduct with an \(R\)-matrix satisfying strict YBE.  The two-point
exchange family \(R_{L_1,L_2}(z)\) and the three-point associator
\(\Phi_\cA\) are both part of the theorem; the Yangian coproduct on
affine evaluation modules is only the binary factorisation component;
and the ordinary braided-monoidal hexagon is assigned to the descended
centre-side module category or a strictified comparison surface.  The
focused line/spectral guard passes with `1 passed`, the full
spectral-braiding suite passes with `25 passed`, the consolidated
harvest-adjacent bundle passes with `127 passed`, `make
verify-licensing` reports zero blockers and zero warnings,
fixed-string stale scans over active spectral and line-operator sources
are clean, and scoped `git diff --check` is clean.

A485 closes the remaining proof-body residue in the spectral-braiding
Part~B argument.  The proof no longer identifies the Part~A object as a
pre-descent braided monoidal category of \(\mathcal H\)-modules with
braiding induced by \(R(z)\).  It now models the local factorisation
line category by \(\mathcal H\)-modules equipped with the spectral
coproduct \(\Delta_z\), the exchange family \(R_{V,W}(z)\), and the
same three-point associator; the braided monoidal category appears only
after evaluation/descent or on a strictified comparison surface.  The
split source also assigns the all-orders YBE statement to a chosen
quantized associator surface rather than to the classical local shadow
alone.  The focused spectral guard passes with `1 passed`, the full
spectral-braiding suite passes with `25 passed`, the harvest-adjacent
subset passes with `64 passed`, the consolidated harvest-adjacent
bundle passes with `127 passed`, `make verify-licensing` reports zero
blockers and zero warnings, fixed-string stale scans are clean, and
scoped `git diff --check` is clean.

A486 closes the Latyntsev comparison residue.  The active core now
defines the Dimofte/Latyntsev surface as a local meromorphic
factorisation tensor datum with products \(\Theta_z\), exchange maps
\(R_{V,W}(z)\), and a three-point associator; it becomes a meromorphic
braided tensor category only after centre-side descent or
strictification.  The split spectral source no longer says that line
operators form a full factorisation quantum group in Latyntsev's
sense, and no longer describes factorisation as a parameter upgrade of
a braided monoidal category.  Both sources now say the spectral data
give only the local shadow; the global Ran-cosheaf and
representation-category statement remains a separate comparison
problem.  The focused spectral guard passes with `1 passed`, the full
spectral-braiding suite passes with `25 passed`, the consolidated
harvest-adjacent bundle passes with `127 passed`, `make
verify-licensing` reports zero blockers and zero warnings,
fixed-string stale scans are clean, and scoped `git diff --check` is
clean.

A492 tightens the \(W_3\) Gaudin/RLL and K3/Hall recognition wording
left adjacent to the Desktop-PDF \(W_3\) correction.  The
\(\mathcal W_3\) Bethe-equation comparison is now explicitly
conjectural until a supplied \(\mathcal W_3\) Gaudin/RLL algebra and
DS--BRST transfer are present.  The pulled-back
\(L^{\Delta_5}\) \(D\)-module sentence is now a target obligation, not
an "expected" theorem.  The \(\mathbf H_{\Delta_5}\) coproduct square is
now an open recognition diagram and remains target-recognition data,
not a construction claim.  The focused W3 guard passes with `1 passed`,
the full input-harvest plus 6d hCS anomaly bundle passes with
`38 passed`, `make verify-licensing` reports zero blockers and zero
warnings, and scoped `git diff --check` is clean.

A493 tightens three remaining theorem-boundary surfaces exposed by the
same input-file discipline.  In `foundations.tex`, the local primitive
object remains theorem-level, but globalization is now exactly
Conjecture~\(\ref{conj:weiss-descent}\) plus the Raskin--Beraldo
categorical Ran descent adaptation to real-oriented boundary blowups;
no global boundary Ran descent theorem is claimed.  In
`concordance.tex`, BV--BRST landing on the chiral Koszul locus is now
the effectiveness clause \(\effKoszul\), an \(\epsilon\)-type datum
stronger than classical Koszulness and separate from (H1)--(H4).  In
`examples-complete-proved.tex`, the standard fingerprint theorem no
longer suggests a non-standard-family extension without extra data:
Monster, logarithmic triplet, and non-freely-generated families require
verified free strong generation plus boundary-linear coset
decomposition, or a sixth twisted-sector coordinate.  The focused A493
guards pass with `2 passed`, the full input-harvest plus 6d hCS anomaly
bundle passes with `39 passed`, `make verify-licensing` reports zero
blockers and zero warnings, and scoped `git diff --check` is clean.

A494 closes the high-visibility summary-layer residue left by the same
input-file discipline.  The preface now states the CY-\(d\) Hochschild
enlargement as sparse support \(\{0,1,2,d\}\), with CY-\(4\) support
\(\{0,1,2,4\}\) and no degree-\(3\) occupancy asserted.  The main
preamble, preface, and introduction now treat
\(\Phi_4^{\mathrm{HK}}\) as a conjectural candidate: DT-\(4\), Nekrasov
\(8d\), and DMVV are comparison targets until twistor descent and the
Costello--Gaiotto 11d lift are constructed.  The K3-AGT equality is now
a comparison problem until the \(\Omega\)-deformed compactification
trace is compared with the Borcherds product character of
\(\mathbf H_{\Delta_5}\).  The helicity observation now carries the
Verdier-helicity and Mellin/Parke--Taylor comparison hypotheses from
Theorem~\(\ref{thm:ch-core-helicity-splitting}\); the mixed-helicity
form-type comparison is not an unconditional consequence of
\(\SCchtop\) alone.  The focused A494 guard passes with `1 passed`, the
full input-harvest guard passes with `35 passed`, the full
input-harvest plus 6d hCS anomaly bundle passes with `40 passed`, the
broader harvest-adjacent cluster passes with `203 passed`, `make
verify-licensing` reports zero blockers and zero warnings, fixed-string
stale scans are clean, and scoped `git diff --check` is clean.

A495 closes the adjacent foundations dictionary residue.  The
five-theorems-as-geometry interpretation now separates the formal
scalar model from the missing global derived-stack lift: the
\(\dfib^2=\kappaChHodge(\cA)\omega_g\) equation suggests a conjectural
geometric lift whose first-order isotropic-failure class should have
that scalar projection, but the Kodaira--Spencer computation is not
asserted.  The self-intersection groupoid paragraph now lives on the
conjectural Koszul--\'etale surface: source/target quasi-isomorphisms
and the Koszul Morita theorem are consequences only after the named
\'etale groupoid and Morita conjectures are supplied.  The focused A495
guard passes with `1 passed`, the full input-harvest plus 6d hCS anomaly
bundle passes with `40 passed`, the broader harvest-adjacent cluster
passes with `203 passed`, `make verify-licensing` reports zero blockers
and zero warnings, fixed-string stale scans are clean, and scoped
`git diff --check` is clean.

A496 strengthens the Chiral Higher Deligne proof body rather than only
scoping it.  The signed two-coloured Boardman--Vogt tree homotopy proof
now prints the one-edge and two-edge sign calculations.  In the one-edge
case it identifies
\(h_{\mathrm{oc}}^{\mathrm{tree}}(T)=(-1)^aT_{e_1}\) and
\(d_{\mathrm{cube}}T_{e_1}=(-1)^a(T-i p(T))\); in the two-edge case it
writes the two interval-insertion terms and the orientation-line
congruence forcing their cancellation.  The main conditional proof no
longer says "the proof obligation here is" after the obstruction class
has been assumed to vanish; it names the extra datum as the
two-coloured homotopy plus vanishing of \([o_{\mathrm{oc}}]\).  The
focused Boardman--Vogt guard passes with `7 passed`, the Chiral Higher
Deligne IV guard passes with `4 passed`, the combined
CHD/input-harvest/hCS bundle passes with `51 passed`, the broader
harvest-adjacent cluster passes with `210 passed`, `make
verify-licensing` reports zero blockers and zero warnings, and scoped
`git diff --check` is clean.

A497 strengthens the adjacent chiral-\(E_3\)-PBW route.  The manuscript
now includes a formal counterexample showing why polynomial growth and
finite amplitude do not replace the page-support clause: a first-quadrant
page with \(E_1^{0,0}=\Bbbk\), \(E_1^{1,0}=\Bbbk\), and
\(E_1^{3,0}=\Bbbk\) has polynomially bounded dimensions, but the
degree-\(3\) class has no incoming total-degree-\(2\) differential and
no outgoing total-degree-\(4\) target, so it survives in a strongly
convergent spectral sequence.  The missing \(E_3\)-PBW input is therefore
the support statement \(E_1^{p,q}=0\) for \(p+q>2\), not merely finite
generation, polynomial growth, or bounded conformal weight.  The focused
Chiral Higher Deligne guard passes with `4 passed`, the combined
CHD/Boardman--Vogt/input-harvest/hCS bundle passes with `51 passed`, the
broader harvest-adjacent cluster passes with `210 passed`, `make
verify-licensing` reports zero blockers and zero warnings, and scoped
`git diff --check` is clean.

A498 closes the adjacent BV/bar comparison residue.  The BV
construction still printed the graph-residue formula for
\(m_k^{\mathrm{BV}}\), but the comparison with
\(m_k^{\mathrm{bar}}\) was too close to a bare equality.  The live text
now inserts a boundary SDR \(i_\partial,p_\partial,h_\partial\),
transfers the graph operations to a coderivation
\(D_A^{\mathrm{BV}}\) on \(T^c(s^{-1}\bar A_\partial)\), and states
the equality only in that transferred boundary model.  The focused BV
bundle passes with `11 passed`, the broader harvest-adjacent cluster
passes with `221 passed`, `make verify-licensing` reports zero blockers
and zero warnings, and scoped `git diff --check` is clean.

A499 sharpens the remaining class-\(\mathsf C\) all-loop BV/bar gap.
The manuscript no longer treats "harmonic decoupling" as an unnamed
condition: it defines the obstruction classes
\(\mathfrak h_{g,r}^{\mathsf C}(\cA)\) by projecting the arity-\(r\)
harmonic comparison defect \(\delta_{g,r}^{\mathrm{harm}}\) to the
quotient by the composite free-field channel
\(\mathcal F_{\mathrm{comp}}^{g,r}(\cA)\).  The genus-one
\(\beta\gamma\) proof is now exactly the statement
\(\mathfrak h_{1,4}^{\mathsf C}(\beta\gamma)=0\); the all-genera
frontier problem is the simultaneous vanishing and compatible lifting
of \(\mathfrak h_{g,r}^{\mathsf C}\) for all \(g\ge1,r\ge4\).  The BV
construction now separates class~C from class~M rather than routing both
through the class~M weight-completed proposition, and the \(W(p)\) PVA
descent surface asks for the same obstruction classes through the
logarithmic stratum.  The focused class-C/BV bundle passes with `15
passed`, the broader harvest-adjacent cluster passes with `225 passed`,
`make verify-licensing` reports zero blockers and zero warnings, and
scoped `git diff --check` is clean.

A500 closes the remaining loose CFG-to-hCS comparison-functor residue.
The appendix now defines the comparison datum
\[
\mathfrak C_{\mathrm{CFG}\to\hCS}
=
(\mathcal R_{\mathrm{ell}},\rho_{\mathrm{BV}},\rho_{\mathrm{fac}},
\tau_{\mathrm{tr}},\{h_n,c_n\}_{n\ge1},
\nu_{\mathrm{Mell}},\eta_{\mathrm{K3}})
\]
and obstruction profile
\(\mathfrak o_{\mathrm{CFG}\to\hCS}\).  The spectral-braiding remark
now uses only the trace component \(\tau_{\mathrm{tr}}\), and the
global theorem is explicitly the existence and compatibility of the
required components, not a consequence of the row-wise comparison
surface.  The focused CFG/hCS guard passes with `3 passed`, the
adjacent harvest bundle passes with `82 passed`, the
half-space/class-C/BV/CFG bundle passes with `60 passed`, `make
verify-licensing` reports zero blockers and zero warnings, and scoped
`git diff --check` is clean.

A501 closes a live fingerprint-classification overclaim exposed by the
same harvest discipline.  The Infinite Fingerprint chapter no longer
asserts that equality of six scalar slots alone forces isomorphism of
all standard-landscape chiral algebras.  It now defines the branchwise
reconstruction datum
\[
\mathfrak R_c\colon
\operatorname{Im}(\varphi'|_{\mathsf{Std}_c})
\to
\{\text{standard-branch chiral algebras in class }c\}/\simeq
\]
and retitles the theorem as a conditional six-slot branchwise
reconstruction criterion.  The \(M_{\mathrm{Kosz}}\) chapter and the
preface/Part~VIII/introduction/main summaries now say that the six
slots become reconstructive only after the corresponding
\(\mathfrak R_c\) is installed.  The focused fingerprint guard passes
with `4 passed`, the adjacent fingerprint/input-harvest bundle passes
with `47 passed`, `make verify-licensing` reports zero blockers and
zero warnings, and scoped `git diff --check` is clean.

A502 closes two active build-surface defects exposed by the A501
verification run.  The Vol~I theorem-package projection table now uses
the defined chiral-bar macro \(\Barch_X\) rather than the undefined
`\Bbarch_X`, and `main.tex` supplies the missing
`DuncanGriffinOno15` bibliography alias for the active umbral
moonshine citation.  The combined bar-cobar/moonshine/fingerprint
bundle passes with `60 passed`, `make verify-licensing` reports zero
blockers and zero warnings, and the final `make fast` converges after
two passes with zero undefined citations, zero undefined references,
and zero rerun requests; the final log scan is clean for fatal errors,
undefined controls, unresolved citation/reference warnings, and rerun
requests.

A503 closes a duplicate-theorem leak in the physical-origin chapter.
The early perturbative localization theorem now carries
\(\ClaimStatusConditional\) with \(\alpha+\gamma\) licensing by the
Costello--Li twist, BV gauge fixing, and renormalization scheme.  The
early boundary-chiral theorem no longer says that an HT boundary
condition by itself supports a chiral algebra; it now fixes a boundary
chart, a renormalized boundary factorization algebra, a
bulk-to-boundary OPE comparison, level normalization, and the
Costello--Dimofte--Gaiotto factorization-to-chiral comparison before
concluding that the boundary local operators form
\(\mathcal A_{\mathrm{bdy}}\).  The focused BV/HT guard passes with
`11 passed`, and the consolidated deletion/input-harvest/BV bundle
passes with `109 passed`.

A504 closes the remaining scalar/full Hodge-lane residue exposed by
the deletion ledger.  The \(M2\) zeta-regularized benchmark now writes
the genus-one convention as
\[
F_1^\zeta=\kappa_{M2}^{\zeta}/24,
\]
not as a bare \(F_1=\kappa/24\).  The actual DDCA self-sewing trace
\(\kappa_{M2}=\kappaChHodge(A_{M2,\infty})\) remains separated from
the regularized benchmark by the zeta-comparison conjecture.  The
focused BV/HT, M2, and scalar-lane guards pass with `13 passed`; a
fixed-string scan over active `chapters`, `appendices`, and
`compute/lib` finds no live `\kappa/24` hits.  `make
verify-licensing` reports zero blockers and zero warnings, and the
final `make fast` converges after two passes with zero undefined
citations, zero undefined references, and zero rerun requests; the
direct final-log scan is clean for fatal errors, undefined controls,
unresolved citation/reference warnings, and rerun requests.

A505 closes a post-harvest proof-status defect in the raviolo
Higgs/Coulomb lane.  The manuscript no longer claims that every
\(3\)d~\(\mathcal N=4\) HT twist satisfying the physics bridge makes
\(H^\bullet(\mathsf V_{\mathrm{rav}},Q)\) simultaneously a Higgs and
Coulomb branch quantization.  It now introduces the explicit datum
\[
\mathfrak h_{\mathcal T}^{\mathrm{rav}}
=(F_H,F_C,\Psi_H,\Psi_C,\psi_{\mathrm{cur}},k_{\mathrm{cur}}),
\]
whose filtrations, associated-graded identifications, transported
Poisson brackets, and current-level map are required before the
Higgs/Coulomb and affine-current conclusions are drawn.  The expanded
raviolo-restriction corollary now requires the same comparison datum.
The focused BV/HT guard passes with `13 passed`; the adjacent BV/HT,
PVA-descent, and raviolo IV bundle passes with `138 passed`; stale
scans over active Vol~II and neighbouring Vol~I/Vol~III roots find no
remaining copies of the old global overclaim.  `make
verify-licensing` reports zero blockers and zero warnings, and the
final `make fast` converges after two passes with zero undefined
citations, zero undefined references, and zero rerun requests; direct
log scan is clean for fatal errors, undefined controls, unresolved
citation/reference warnings, and rerun requests.

A506 closes a native Theorem C/D proof-status defect.  The global
shifted-symplectic Theorem C upgrade now requires the total gluing
datum
\[
\mathfrak s_{\mathcal A}^{C}
=
(\mathcal Q_{g,n}(\mathcal A),\mathcal Q_{g,n}(\mathcal A^!),
\langle-,-\rangle_{\mathrm{Verd}},
\theta_{g,n}^{\mathrm{PTVV}},\{\Lambda_\xi\}_\xi,\sigma_{\mathcal A}),
\]
which supplies the PTVV orientation, closedness, clutching
Lagrangian correspondences, and compatible diagonal section.  The
tensor-Arakelov Theorem D upgrade now requires the graph datum
\[
\mathfrak k_{\mathcal A}^{D}
=
(\mathcal F,\{P_{\alpha\beta}\},\{C_v\},
\{\mathsf G^{\mathrm{st}}_{g,2}(\alpha,\beta)\},
\operatorname{or}_{\Gamma},\tau_{\mathrm{FP}}),
\]
which supplies the finite weight-bundle, channel propagators, vertex
tensors, stable graph orientations, convergence/regularisation data,
and Faber--Pandharipande trace functional.  The scalar Vol~I formulas
remain the diagonal input; they no longer determine the global
shifted-symplectic or full tensor package by themselves.  The focused
modular graph/Arakelov/tensor, scalar-tensor deletion, and
\(W_N\) tensor guards pass with `9 passed`, and propagation scans over
active Vol~II and neighbouring Vol~I/Vol~III roots find no surviving
unconditional C/D upgrade phrases.

A487 closes the remaining Hopf/FQG object-typing residue.  The
Koszul-dual boundary object \(\mathcal H\) is now a filtered local
bialgebra shadow encoding the line-operator OPE and higher products,
not the factorisation quantum group of the theory.  Hopf or
factorisation-quantum-group interpretation now explicitly requires the
additional spectral coproduct, associator, descent, and global
comparison data.  The split theorem no longer identifies
\(\mathcal H\) parenthetically with the factorisation quantum group;
it says \(r(z)\) gives the filtered quasi-triangular local shadow.  The
strictification computation now yields a strict local spectral tensor
datum on the resolved affine comparison surface for simple \(\fg\), not
a global strict factorisation quantum group.  The focused spectral
guard passes with `1 passed`, the full spectral-braiding suite passes
with `25 passed`, the consolidated harvest-adjacent bundle passes with
`127 passed`, `make verify-licensing` reports zero blockers and zero
warnings, fixed-string stale scans are clean, and scoped
`git diff --check` is clean.

A488 repairs the formula-level strict-inverse residue in the proof of
the field-theoretic \(r(z)\)-formula.  The CYBE is now derived from the
associator-controlled Yang--Baxter relation with
\(R(z)=1+\hbar r(z)+O(\hbar^2)\) and
\(\Phi_A=1+O(\hbar^2)\), or from strict QYBE only after strictification.
The skew relation \(r_{12}(z)+r_{21}(-z)=0\) is no longer attributed to
the definition of braiding; it follows only when the chosen comparison
surface carries the unitary exchange normalization
\(R_{12}(z)R_{21}(-z)=1\).  The focused spectral guard passes with
`1 passed`, the full spectral-braiding suite passes with `25 passed`,
the consolidated harvest-adjacent bundle passes with `127 passed`,
`make verify-licensing` reports zero blockers and zero warnings,
fixed-string stale scans are clean, and scoped `git diff --check` is
clean.

A489 closes the remaining local-exchange/descent residue left by the
same PDF correction layer.  The spectral definition now has source and
target \(L_1\otimes_z L_2\to L_2\otimes_{-z}L_1\), not a fixed tensor
product.  The Laplace transform is the local bridge from
\(\lambda\)-brackets to the open-colour exchange shadow, not the whole
spectral duality of the HT theory.  The DK comparison is reached only
after fixing the affine evaluation surface, KZ/Drinfeld normalization,
associator, and descent.  Skewness in the \(A_\infty\) Yang--Baxter
theorem and dg-shifted Yangian definition is now the normalized
consequence of \(R_{12}(z)R_{21}(-z)=1\), not a formal symmetry of the
tensor product.  The Swiss-cheese ordered-to-symmetric descent now
assumes a braid-descent datum and unitary exchange normalization before
forming the \(\Sigma_n\)-equivariant local system.  The HT and THQG line
surfaces now locate full braided monoidal/QYBE statements only on
descended or strictified comparison surfaces.  The focused spectral
guard passes with `1 passed`, the full spectral-braiding suite passes
with `25 passed`, the consolidated harvest-adjacent bundle passes with
`127 passed`, `make verify-licensing` reports zero blockers and zero
warnings, fixed-string stale scans are clean, and scoped
`git diff --check` is clean.

A490 propagates the same correction through the active introduction,
ordered-KD descent theorem, spectral frontier, ordered-KD frontier, and
the THQG spectral extension.  The dg-shifted Yangian synopsis no longer
lists \(r(-z)=-r_{21}(z)\) as bare weak co-commutativity; it is a
normalized consequence only when the unitary exchange normalization is
part of the comparison surface.  The ordered-to-symmetric descent now
requires a braid-descent datum: associator trivialization on triple
overlaps and unitary exchange normalization on adjacent exchange
collars.  The proof no longer derives strong unitarity from symmetry of
the leading Casimir term.  Frontier comparison paragraphs now state the
associator-controlled Yang--Baxter relation first and the strict
equation only after strictification.  The THQG spectral extension now
records that \(R_{12}(z)R_{21}(-z)=1\) is an added
comparison-surface datum and that tensor-skewness is a separate
condition, incompatible with a nonzero symmetric simple-pole Casimir
kernel.  The focused spectral guard passes with `1 passed`, the full
spectral-braiding suite passes with `25 passed`, ordered-KD plus input
harvest guards pass with `90 passed`, the consolidated harvest-adjacent
bundle with ordered-KD guards passes with `185 passed`, `make
verify-licensing` reports zero blockers and zero warnings, fixed-string
stale scans are clean, and scoped `git diff --check` is clean.

A491 closes a remaining status-surface drift in the harvested all-loop
BV/BKM and Drinfeld-centre corrections.  The 6d hCS coefficient chapter
now marks the \(n\le 3\) graph-complex coefficient calculations as the
proved scalar surface, marks the CFG comparison and BKM lifts as
conditional, and retitles the all-loop section as the
All-loop BV anomaly conjecture.  The all-loop hCS statement now carries
\(\ClaimStatusConjectured\) with licensing tags
\(\beta+\gamma+\epsilon\), and the text explicitly states that no
all-loop theorem is claimed without the finite Hall/BV source, hCS
chain map, parity/root fixtures, denominator comparison, and graph
orientation data.  The Hochschild Drinfeld-centre lead-in now says that
the comparison with the chiral derived centre is conjectured in the
labelled conjecture, not merely "expected" and not theorem-level
background.  The focused A491 guards pass with `2 passed`, the full
input-harvest plus 6d hCS anomaly bundle passes with `38 passed`,
`make verify-licensing` reports zero blockers and zero warnings,
fixed-string stale scans are clean, and scoped `git diff --check` is
clean.
